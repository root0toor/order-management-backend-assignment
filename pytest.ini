[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short

# Markers for test categorization
markers =
    unit: unit tests
    integration: integration tests
    scheduler: scheduler tests
    business_rules: tests focused on business rules
